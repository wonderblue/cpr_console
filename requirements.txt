# CPR Screening Console - Dependencies
# Install with: pip install -r requirements.txt

streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
pytz>=2023.3
requests>=2.31.0
python-dateutil>=2.8.2

# Optional: for enhanced CSV handling
# openpyxl>=3.1.0

yfinance>=1.0.0
duckdb>=1.0.0
pyarrow>=15.0.0

# Live quotes come from Yahoo Finance via yfinance (NSE .NS tickers).
# No API key required. Data may be delayed. Not for live trading.